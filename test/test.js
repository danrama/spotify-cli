'use strict';

var expect = require('chai').expect;
var spotifyCli = require('../index');

describe('#spotifyCli', function() {
    it('should TBD', function() {
        var result = null;
        expect(result).to.equal('1');
    });
});
